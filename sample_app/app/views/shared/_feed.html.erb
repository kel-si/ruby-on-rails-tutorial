<% if @feed_items.any? %>
    <ol class="microposts">
        <%# Rails knows to call micropost partial because each element of @feed_items has class Micropost %>
        <%= render @feed_items %>
    </ol>
    <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>